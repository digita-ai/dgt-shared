export enum DGTEmailTemplateType {
    MILESTONE_ARCHIVED = 'milestone-archived',
    MILESTONE_CONFIRMED = 'milestone-confirmed',
    INVITE_CREATED = 'invite-created',
    UPDATE_CONFIRMED = 'update-confirmed',
    UPDATE_CREATED = 'update-created',
    UPDATE_REJECTED = 'update-rejected',
}
